pluginManagement {
    repositories {
        maven {
            url 'https://oss.sonatype.org/content/repositories/snapshots/'
        }
        gradlePluginPortal()
        mavenCentral()
        maven {
            url "https://maven.pkg.jetbrains.space/kotlin/p/kotlin/bootstrap/"
        }
    }
}

include ':app', ':mvvm', ':annotation', ':compiler', ':utils', ':ksp-compiler'